/* eslint-disable react-hooks/set-state-in-effect */
"use client";
import { useState, useEffect, JSX } from "react";
import { useRouter } from "next/navigation";
import { ClipboardCheck, Users, Building2, LogOut, Menu, ChevronLeft, Settings } from "lucide-react";
import ChecksTab from "./components/ChecksTab";
import CustomersTab from "./components/CustomersTab";
import DriversTab from "./components/DriversTab";
import AdminSettingsTab from "./components/AdminSettingsTab";
type TabType = "checks" | "drivers" | "customers" | "settings";
const NavButton=({onClick,isActive,isSidebarOpen,icon,label}:{onClick:()=>void;isActive:boolean;isSidebarOpen:boolean;icon:JSX.Element;label:string})=><button onClick={onClick} className={`flex w-full items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${isActive?"bg-[#042433] text-white shadow-sm":"text-slate-600 hover:bg-slate-100"}`} title={!isSidebarOpen?label:""}><div className="flex w-5 shrink-0 justify-center">{icon}</div><span className={`whitespace-nowrap ${isSidebarOpen?"w-[120px] opacity-100":"w-0 opacity-0 overflow-hidden"}`}>{label}</span></button>;
export default function AdminDashboardPage():JSX.Element{const router=useRouter();const[activeTab,setActiveTab]=useState<TabType>("checks");const[selectedCustomerId,setSelectedCustomerId]=useState("");const[isSidebarOpen,setIsSidebarOpen]=useState(true);const[loading,setLoading]=useState(true);useEffect(()=>{try{const raw=localStorage.getItem("ts_user_session")||localStorage.getItem("currentUser");if(!raw){router.replace("/");return}const s=JSON.parse(raw) as{role?:string};if(s.role!=="admin"){router.replace("/");return}setLoading(false)}catch{localStorage.removeItem("ts_user_session");localStorage.removeItem("currentUser");router.replace("/")}},[router]);useEffect(()=>{const titles:Record<TabType,string>={checks:"Журнал осмотров",drivers:"Водители",customers:"Заказчики",settings:"Настройки"};document.title=`${titles[activeTab]} — Транспортный щит`},[activeTab]);useEffect(()=>{const h=(e:Event)=>{const n=(e as CustomEvent<string>).detail;if(n){setSelectedCustomerId(n);setActiveTab("drivers")}};window.addEventListener("transport-shield:open-customer",h);return()=>window.removeEventListener("transport-shield:open-customer",h)},[]);useEffect(()=>{if(activeTab!=="drivers"||!selectedCustomerId)return;const t=window.setTimeout(()=>{const i=document.querySelector('input[placeholder="Поиск по ФИО, номеру, авто..."]') as HTMLInputElement|null;if(!i)return;const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,"value")?.set;setter?.call(i,selectedCustomerId);i.dispatchEvent(new Event("input",{bubbles:true}));i.dispatchEvent(new Event("change",{bubbles:true}))},100);return()=>window.clearTimeout(t)},[activeTab,selectedCustomerId]);const logout=()=>{localStorage.removeItem("currentUser");localStorage.removeItem("ts_user_session");router.push("/")};if(loading)return <div className="flex h-screen items-center justify-center bg-slate-50 text-slate-500">Проверка доступа...</div>;return <div className="flex h-screen flex-col overflow-hidden bg-slate-50 text-slate-900 md:flex-row"><aside className={`hidden md:flex ${isSidebarOpen?"w-64":"w-16"} flex-col border-r border-slate-200 bg-white shrink-0`}><div className="flex h-16 items-center justify-between border-b border-slate-100 px-3"><div className={`${isSidebarOpen?"flex":"hidden"} items-center gap-3`}><img src="/logo.png" alt="Logo" className="h-8 w-8 rounded-lg object-contain"/><span className="text-sm font-bold">Транспортный Щит</span></div><button onClick={()=>setIsSidebarOpen(!isSidebarOpen)} className="flex h-8 w-8 items-center justify-center rounded-lg hover:bg-slate-100">{isSidebarOpen?<ChevronLeft size={20}/>:<Menu size={20}/>}</button></div><nav className="flex-1 space-y-1 p-2"><NavButton isSidebarOpen={isSidebarOpen} isActive={activeTab==="checks"} onClick={()=>setActiveTab("checks")} icon={<ClipboardCheck size={18}/>} label="Журнал осмотров"/><NavButton isSidebarOpen={isSidebarOpen} isActive={activeTab==="drivers"} onClick={()=>{setSelectedCustomerId("");setActiveTab("drivers")}} icon={<Users size={18}/>} label="Водители"/><NavButton isSidebarOpen={isSidebarOpen} isActive={activeTab==="customers"} onClick={()=>setActiveTab("customers")} icon={<Building2 size={18}/>} label="Заказчики"/><NavButton isSidebarOpen={isSidebarOpen} isActive={activeTab==="settings"} onClick={()=>setActiveTab("settings")} icon={<Settings size={18}/>} label="Настройки"/></nav><button onClick={logout} className="m-2 flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-slate-600 hover:bg-red-50 hover:text-red-600"><LogOut size={18}/><span className={isSidebarOpen?"":"hidden"}>Выйти</span></button></aside><main className="flex-1 overflow-y-auto p-4 md:p-6"><div className="mx-auto max-w-7xl">{activeTab==="checks"&&<ChecksTab/>}{activeTab==="drivers"&&<DriversTab/>}{activeTab==="customers"&&<CustomersTab/>}{activeTab==="settings"&&<AdminSettingsTab/>}</div></main></div>}
