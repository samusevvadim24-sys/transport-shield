import { CheckCircle2, Clock } from "lucide-react";

interface Props { latestCheck:any; inspectionAddress:string; statusStyles:Record<string,string>; getDisplayStatus:(status?:string,address?:string)=>string; hasExpiredDocs:boolean; timeRemaining:number|null; isSubmitting:boolean; onRequest:()=>void; }

export default function DriverActionBlock({latestCheck,inspectionAddress,statusStyles,getDisplayStatus,hasExpiredDocs,timeRemaining,isSubmitting,onRequest}:Props){const status=latestCheck?.overall_status;const waiting=status==="Ожидание";const summoned=status==="Явиться";return <div className="space-y-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><h3 className="text-xs font-semibold uppercase tracking-wider text-slate-500">Действия</h3>{latestCheck&&<span className={`max-w-[80%] rounded-full border px-2.5 py-1 text-right text-xs font-semibold ${statusStyles[status]||"border-slate-200 bg-slate-50 text-slate-600"}`}>{getDisplayStatus(status,inspectionAddress)}</span>}</div>{waiting?<div className="flex items-center gap-2 rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-xs font-medium text-amber-700"><Clock size={18} className="animate-pulse"/><span>Ваш запрос обрабатывается. Пожалуйста, ожидайте.</span></div>:summoned?<div className="rounded-lg border border-amber-200 bg-amber-50 px-4 py-3 text-xs font-medium text-amber-800">{getDisplayStatus(status,inspectionAddress)}</div>:timeRemaining&&timeRemaining>0?<div className="rounded-lg border border-slate-200 bg-slate-50 px-4 py-3 text-center"><Clock size={18} className="mx-auto text-slate-400"/><span className="mt-1 block text-[10px] text-slate-400">До следующего осмотра</span><b className="text-sm text-slate-800">{Math.floor(timeRemaining/3600000)} ч. {Math.floor((timeRemaining%3600000)/60000)} мин.</b></div>:hasExpiredDocs?<div className="rounded-lg border border-[#C53030]/20 bg-[#C53030]/10 px-4 py-3 text-center text-xs font-medium text-[#C53030]">Запрос недоступен: имеются просроченные документы</div>:<button type="button" disabled={isSubmitting} onClick={onRequest} className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#042433] px-4 py-2.5 text-sm font-medium text-white shadow-sm hover:bg-[#031d29] disabled:opacity-70">{isSubmitting?<span className="h-4 w-4 animate-spin rounded-full border-2 border-white/30 border-t-white"/>:<CheckCircle2 size={16}/>} {isSubmitting?"Отправка...":"Пройти осмотр"}</button>}</div>}
